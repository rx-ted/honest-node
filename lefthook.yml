pre-commit:
  parallel: true
  commands:
    format:
      run: pnpm format
    lint:
      run: pnpm lint
    check:
      run: pnpm check
    build:
      run: pnpm build

post-commit:
  commands:
    bump-version:
      run: node scripts/bump-version.mjs {1}
